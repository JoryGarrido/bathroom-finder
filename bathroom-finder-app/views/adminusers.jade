extends layout
block content

  h1 Admin page
  table
    tr
      th User name
    each user in users
      tr
        td= user.username
        td
          a(href="/admin/users/#{user.id}/delete") Delete
        
